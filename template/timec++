#include <bits/stdc++.h>
using namespace std;

#define LL long long
#define VI vector<int>
#define VLL vector<LL>
#define mp make_pair
#define pb push_back
typedef pair<int, int> PII;
typedef pair<LL, LL> PLL;
#define A first
#define B second
#define io ios_base::sync_with_stdio(false)

void print_time(const clock_t& begin) {
    clock_t end = clock();
    double elapsed_secs = double(end - begin) / CLOCKS_PER_SEC;
    cout << "elapsed time: ";
    cout << fixed << setprecision(4) << elapsed_secs << "\n";
}

const int INF = 1e9;

int main() {
#ifdef TIMP
    clock_t start_time = clock();
#endif
    io;


#ifdef TIMP
    print_time(start_time);
#endif
    return 0;
}
